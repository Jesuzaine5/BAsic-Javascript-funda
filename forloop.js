let people = ["Lisa", "Jennie", "Jisoo","Rose"];  //this is a sample of array

for(let i = 0; i < people.length; i++){ // for(declaration; condition; operato) // its still the same(LET i = people.length-1; i >=0; i++)
    console.log(people[i]);  
    
}